{
  "name": "pedrokart",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "test": "node tools/ci-smoke.mjs",
    "lint": "for f in server.js public/*.js public/maps/*.js tools/*.mjs; do node --check \"$f\" || exit 1; done",
    "check-maps": "for f in public/maps/*.js; do node tools/check-map.mjs \"$f\" > /dev/null || { node tools/check-map.mjs \"$f\"; exit 1; }; done && echo all tracks OK"
  },
  "type": "module",
  "private": true,
  "dependencies": {
    "qrcode": "^1.5.4",
    "three": "^0.186.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "playwright": "1.55"
  }
}
